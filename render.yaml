services:
  - type: web
    name: base44-api
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: npm run start -w @base44/server
    envVars:
      - key: PORT
        value: 3000
    routes:
      - path: /api
        destination: /api
